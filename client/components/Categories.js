import Link from 'next/link';
import SectionHeading from './SectionHeading';
import { categories } from '@/data/site';
export default function Categories() { return <section className="section-pad"><div className="container-width"><SectionHeading title="Shop by Category" subtitle="Find the perfect gift for every occasion and relationship." link="View All Categories →"/><div className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-6">{categories.map(item => <Link href={`/shop?category=${encodeURIComponent(item.name)}`} key={item.name} className="group overflow-hidden rounded-lg border border-slate-100 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="flex h-32 items-center justify-center bg-[#fff2ee] text-6xl transition group-hover:scale-105">{item.name.includes('Birthday')?'🎂':item.name.includes('Anniversary')?'💝':item.name.includes('Corporate')?'🧳':item.name.includes('Personalized')?'☕':item.name.includes('Wedding')?'🎁':'🪔'}</div><div className="p-4 text-center"><h3 className="text-sm font-bold">{item.name}</h3><p className="mt-2 text-xs font-bold text-[#c92532]">Shop Now →</p></div></Link>)}</div></div></section>; }
