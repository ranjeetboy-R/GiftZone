'use client';
import { useEffect,useState } from 'react'; import ProductCard from './ProductCard'; import SectionHeading from './SectionHeading'; import { apiFetch } from '@/lib/api'; import { demoProducts as fallbackProducts } from '@/data/site';
export default function BestSellers(){const [products,setProducts]=useState(fallbackProducts); useEffect(()=>{apiFetch('/api/products?featured=true').then(data=>{if(data.products?.length)setProducts(data.products.slice(0,8));}).catch(()=>{});},[]); return <section className="section-pad"><SectionHeading eyebrow="Our Favorites" title="Best Sellers" text="Thoughtfully selected gifts that customers love to share."/><div className="container-width grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{products.map(product=><ProductCard key={product._id||product.id||product.slug||product.name} product={product}/>)}</div></section>}
